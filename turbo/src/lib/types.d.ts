export interface User {
  id: string;
}

export interface Claim {
  id: string;
  topicName: string;
  subtopicName: string;
  interview: string;
  quote: string;
  claim: string;
  commentId: string;
}
